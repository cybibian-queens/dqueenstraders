/**
 * Stub for the shared data-table row type.
 * The original data-table component was not migrated to the shared_ui directory;
 * these types satisfy the imports in shared_ui/data-list/data-list-row.tsx.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type TSource = Record<string, any>;

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type TColIndex = string | number;
