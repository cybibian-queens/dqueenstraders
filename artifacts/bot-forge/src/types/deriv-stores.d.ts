// Stub for @deriv/stores/types — the package is not on npm;
// types are inlined here so imports compile without the package.
declare module '@deriv/stores/types' {
    export type TStores = Record<string, unknown>;
    export type TRootStore = Record<string, unknown>;
}
